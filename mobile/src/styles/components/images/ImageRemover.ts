import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 5,
    opacity: 0.6,
  },
  icon: {
    fontSize: 20,
    color: 'red',
  },
});

export default styles;
