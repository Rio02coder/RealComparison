import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  error: {
    color: 'red',
    alignSelf: 'center',
    marginTop: 2,
    fontSize: 12,
    fontWeight: 'bold',
  },
});

export default styles;
