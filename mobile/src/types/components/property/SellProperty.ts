import {SpecificProperty} from '../../Property';
import ReduxProps from '../../redux/props';

type SellPropertyProps = {
  property: SpecificProperty;
  reduxProps: ReduxProps;
};

export default SellPropertyProps;
