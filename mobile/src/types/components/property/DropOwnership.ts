import ReduxProps from '../../redux/props';
import {Property} from '../../Property';

type DropOwnershipProps = {
  reduxProps: ReduxProps;
  property: Property;
};

export default DropOwnershipProps;
