import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  presentation: {
    alignSelf: 'center',
    marginTop: 20,
    color: '#a8a4a4',
    fontSize: 8,
    fontWeight: 'bold',
  },
});

export default styles;
