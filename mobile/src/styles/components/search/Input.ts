import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 50,
    width: '70%',
    color: 'white',
    fontSize: 25,
    fontWeight: '500',
    marginLeft: 5,
  },
});

export default styles;
